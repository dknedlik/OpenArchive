[package]
name = "open_archive"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
chrono = { version = "0.4", features = ["serde"] }
clap = { version = "4.5", features = ["derive"] }
oracle = { version = "0.6", features = ["chrono"] }
sha2 = "0.10"
