id: "17"
title: Add end-to-end first-slice validation
assignee: david
status: todo
type: test
priority: high
planned_order: 16
milestone: M1.first_vertical_slice
labels: [testing, e2e, slice-validation]
depends_on: ["08", "09", "12", "13", "14", "15"]
description: |
  Add the final slice-one validation coverage that proves the full path from
  ChatGPT import through durable enrichment and `conversation_resume`
  retrieval against the current local Oracle-backed setup.
acceptance_criteria:
  - One end-to-end test or equivalent validation flow covers import, durable job creation, worker execution, derived metadata persistence, and context-pack retrieval.
  - The validation proves provenance-bearing outputs rather than only happy-path HTTP status codes.
  - The slice validation covers not-ready or partial behavior explicitly.
  - The local ADB-backed development path remains runnable with the current wallet/client setup.
files_touched:
  - tests/
  - scripts/
  - docs/
implementation_plan: |
  - Build one repeatable slice-validation flow using the existing test harness or a narrow integration driver.
  - Verify both returned API payloads and persisted database state.
  - Keep the flow narrow and deterministic so it can serve as the milestone exit check.
test_plan: |
  - Exercise one successful slice and at least one not-ready or partial-enrichment case.
  - Verify the final `conversation_resume` includes evidence-backed excerpts and provenance fields.
risks: |
  - Without a true end-to-end proof, slice one may appear complete while key seams only work in isolation.
notes: |
  - This task is the milestone-close validation, not a replacement for narrower unit/integration tests.
